output: dist
name: duel_map

require:
  - bolt
  - bolt_expressions
  - plugins.macro_interpolation

data_pack:
  name: 'Duel Map Datapack'
  pack_format: 48
  load: 'src'

pipeline:
  - mecha

meta:
  bolt:
    prelude:
      - 'tools:prelude/prelude'
    entrypoint:
      - 'pvp:main'
    
